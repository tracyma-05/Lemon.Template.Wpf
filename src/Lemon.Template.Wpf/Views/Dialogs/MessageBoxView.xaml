<UserControl x:Class="Lemon.Template.Wpf.Views.Dialogs.MessageBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lemon.Template.Wpf.Views.Dialogs"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:dialog="clr-namespace:Lemon.Template.Wpf.Infrastructures.Dialogs"
             mc:Ignorable="d" 
             Width="450"
             Height="200"
             Background="{DynamicResource MaterialDesignCardBackground}">
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid Margin="20" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon
                    Width="40"
                    Height="40"
                    VerticalAlignment="Bottom"
                    Kind="Information" />
                <TextBlock
                    Margin="10,0,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    d:Text="This is a warning message"
                    FontSize="16"
                    FontWeight="Bold"
                    Text="{Binding Title}" />
            </StackPanel>

            <TextBlock
                Grid.Row="1"
                Margin="50,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                d:Text="Alert message content！"
                Opacity="0.6"
                Text="{Binding Message}"
                TextWrapping="Wrap" />
        </Grid>

        <StackPanel
            Grid.Row="1"
            Margin="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Orientation="Horizontal">
            <Button Command="{Binding SaveCommand}" Content="Yes" />
            <Button
                Margin="10,0,0,0"
                Command="{Binding CancelCommand}"
                Content="No" />
        </StackPanel>
    </Grid>
</UserControl>
