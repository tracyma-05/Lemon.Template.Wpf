<UserControl x:Class="Lemon.Template.Wpf.Views.Settings.LanguageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:loc="clr-namespace:Lemon.Template.Wpf.Infrastructures.Localization"
             mc:Ignorable="d"
             d:DesignHeight="480"
             d:DesignWidth="960">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24" MaxWidth="720">
            <TextBlock Text="{loc:Localize Language_Title}"
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                       Margin="0,0,0,8" />
            <TextBlock TextWrapping="Wrap"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       Text="{loc:Localize Language_Description}" />

            <materialDesign:Card Margin="0,16,0,0" Padding="16">
                <ComboBox ItemsSource="{Binding Languages}"
                          SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}"
                          DisplayMemberPath="DisplayName"
                          materialDesign:HintAssist.Hint="{loc:Localize Language_SelectorHint}"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}" />
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
